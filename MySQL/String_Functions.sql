SELECT CHAR_LENGTH("Vijay") AS LengthOfString;

SELECT CONCAT_WS("Session ", "is ", "going ", "on!") AS ConcatenatedString;

Sorting by multiple columns
Order by
Subquery create table using as
Alter table ADD MODIFY DROP
Limit
Auto commit
Manual commmit
